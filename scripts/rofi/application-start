# rofi - dmenu replacement
## called via mod+d
## open windows, connect ssh, run applications
## config file: ~/.config/rofi/config

rofi -modi run -show run -eh 1 -auto-select -theme-str 'listview {lines: 5;}'
