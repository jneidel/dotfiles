# alsa config

# use 'aplay -l' to check for available sound cards

pcm.!default {
  type hw
  card 1
}
ctl.!default {
  type hw
  card 1
}

# Enables multiple applications to ouput sound at the same time
pcm.dsp {
  type plug
  slave.pcm "dmix"
}
